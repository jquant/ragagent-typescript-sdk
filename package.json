{
  "name": "@jquant/ragagent-client",
  "version": "1.0.0",
  "publishConfig": { 
    "registry": "https://npm.pkg.github.com",
    "access": "public" 
  },
  "description": "OpenAPI client for ragagent-client",
  "author": "JAI Team",
  "repository": {
    "type": "git",
    "url": "https://github.com/jquant/ragagent-typescript-sdk.git"
  },
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "module": "./dist/esm/index.js",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && tsc -p tsconfig.esm.json",
    "prepare": "npm run build",
    "test": "echo \"Tests not implemented yet\" && exit 0",
    "lint": "echo \"Linting not implemented yet\" && exit 0",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^4.0 || ^5.0"
  }
}
